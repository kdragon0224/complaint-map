# PRD — AI 민원배부 내비게이션
> Product Requirements Document (기술 레퍼런스)
> 작성: 한국도로공사 전북본부 | 최초 작성: 2026년 6월 | 최종 수정: 2026년 8월 29일 (전국 IC·JC·TG·SA 908개 시설 앵커 + 전국 램프 형상 11,533개 세그먼트 추가로 인터체인지 루프 오탐 근본 해결, 고속도로 우선판정 생략 기준 200m→50m 축소, 제2경인연결고속도로(9106) 추가, 시설 앵커 좌표를 카카오 지오코딩에서 data.ex.co.kr 도로공사 공식 OpenAPI(IC·SA·TG 1,235건)로 교체, 민자 고속도로 19개 노선 좌표를 국토교통부 표준노드링크(23,097건)로 재교체)

---

## 1. 기술 스택

| 구분 | 기술 |
|------|------|
| 프레임워크 | Next.js 16 (App Router, TypeScript) |
| 스타일 | Tailwind CSS v4 |
| 지도 | Kakao Maps JavaScript SDK (`libraries=services`, `autoload=false` + async) |
| 주소검색 | Kakao Local REST API (주소검색 + 키워드검색 병렬 실행) |
| 행정구역 | Kakao coord2regioncode API (도로법 규칙 판정용) |
| DB | Supabase (PostgreSQL) — 오류 신고 게시판, 이용 로그 |
| 배포 | GitHub → Vercel 자동 배포 (main 브랜치 push 시 즉시 반영) |
| 도메인 | https://complaint-map.vercel.app |

---

## 2. 아키텍처: 도로 인식 ↔ 관리주체 판정 분리

이 앱이 하는 일은 크게 두 단계로 나뉜다. **① 사용자가 찍은 위치가 어떤 도로인지 찾는 것**(도로 인식)과,
**② 그 도로를 실제로 누가 관리하는지 정하는 것**(관리주체 판정)이다. 이 둘을 의도적으로 분리해서 처리한다 —
도로 인식은 순수하게 "좌표 사이의 거리 계산"만으로 끝나고, 관리주체 판정은 별도의 규칙 표들을 정해진
순서대로 확인하는 방식이다. 아래에서 실제 처리 순서를 하나씩 설명한다.

### 2-0. 처리 흐름 (단계별 설명)

**1) 위치 확정 — 지오코딩**
사용자가 주소, IC·JC명, 또는 지도 위 핀 이동으로 위치를 알려주면, 이걸 정확한 좌표(위도·경도)로
바꿔야 한다. 카카오의 **주소검색 API**와 **키워드검색 API**를 동시에(병렬로) 호출한다.
- 주소검색 결과에는 좌표뿐 아니라 행정구역(시/도·시/군/구·동)까지 같이 들어있어서, 뒤에서
  행정구역이 필요할 때 API를 또 호출하지 않아도 된다.
- 키워드검색 결과 중에서는 고속도로 시설(IC·JC·휴게소)을 우선으로 정렬해서, "장수IC"처럼
  시설 이름으로 검색했을 때 엉뚱한 상호명(예: "장수IC주유소")보다 실제 IC가 먼저 뜨게 한다.
- **동명이지 처리**: 간혹 같은 지번 주소가 서로 다른 시/도에 동시에 존재한다(예: "산 57-4"라는
  지번이 경기도와 경상남도에 둘 다 있는 식). 이럴 때 아무거나 자동으로 고르면 엉뚱한 위치로
  판정될 위험이 있으므로, 시스템은 자동 선택하지 않고 후보 목록을 사용자에게 보여준다. 이 경우
  API는 평소의 결과 대신 `{ ambiguous: candidates[] }`(직역하면 "위치가 모호하니 후보 목록을
  보세요"라는 뜻의 응답)를 돌려주고, 화면에는 선택 가능한 후보 리스트가 뜬다 (자세한 내용은 §15).

**2) 고속도로인지 확인**
좌표가 정해지면 가장 먼저 "여기가 고속도로 위인가"부터 확인한다. 이때 쓰는 데이터가
`highway-grid.json`인데, 전국 고속도로의 중심선을 따라 약 300~500m 간격으로 미리 찍어둔
좌표점 약 6만 개의 모음이다. 사용자가 입력한 좌표에서 **반경 500m 이내**에 있는 점들을 찾아
그중 가장 가까운 걸 채택한다.
- 이 6만 개 점을 매번 다 훑으면 느리기 때문에, 전국을 위경도 0.1도(약 11km) 크기 격자로 미리
  나눠서 저장해뒀다. 그래서 검색할 때는 입력 좌표 주변 격자 몇 칸만 확인하면 충분하다.
- **왜 50m 이내면 다음 단계(국도·지방도 탐색)를 생략하는가**: 국도·지방도 데이터 파일
  (`osm-road-grid.json`)은 12만7천여 개 포인트가 든 훨씬 큰 파일이라 매번 파싱하면 느리다.
  고속도로가 아주 코앞(50m 이내)에 있다면 사실상 그 지점은 고속도로 위라고 봐도 무방하므로,
  불필요한 추가 탐색을 생략해서 응답 속도를 높인다.
  - **2026-08-21 200m → 50m로 축소**: 경춘로(국도6호선)처럼 고속도로 바로 옆/밑을 지나는
    국도가 실제로 더 가까운데도(예: 경춘로 40m vs 수도권제1순환선 74m), 200m 기준 때문에
    국도 쪽은 아예 확인도 안 하고 고속도로로 확정해버리는 사례를 발견해 기준을 좁혔다.
    파일은 §2-0과 동일하게 서버 시작 시 한 번만 읽어 메모리에 캐시하므로(Vercel 서버리스의
    콜드 스타트 직후 첫 요청만 예외), 국도·지방도 탐색이 더 자주 실행돼도 요청당 속도에는
    체감될 영향이 없다.

**3) 고속도로가 아니면 국도·지방도 확인**
고속도로가 없거나 200m보다 멀면 `osm-road-grid.json`을 마저 탐색한다. 이 데이터는
OpenStreetMap(전 세계 자원봉사자들이 만드는 오픈소스 지도 데이터)의 한국 도로 정보에서,
**국도**(도로 번호 1~99번대, 내부적으로 `n`으로 표기)·**지방도**(100번대 이상, `p`)·
**이름은 있지만 번호가 없는 주요 간선도로**(올림픽대로 같은 도로, `x`)를 추출한 것이다.
고속도로용 데이터와 마찬가지로 0.1도 격자로 미리 정리해뒀고, 탐색 방식(반경 500m)도 동일하다.

**4) 관리주체(어느 기관이 담당인지) 판정**
어떤 도로인지 확인됐으면, 이제 "이 구간을 누가 관리하는가"를 정한다. 판정 방식은 고속도로냐
아니냐에 따라 완전히 다른 규칙을 쓴다 — 자세한 우선순위와 근거자료는 바로 아래 §2-1에서 설명한다.
- 고속국도인 경우: `highway-jurisdiction.ts`의 4단계 규칙을 순서대로 확인
- 그 외(국도·지방도·간선)인 경우: `road-rules.ts`의 규칙을 확인 — 이때는 좌표만으로는
  부족해서 카카오 API로 행정구역(시/도·동)을 추가로 조회한다
- 아무 도로도 인식하지 못했으면 "OO시 도로관리부서 (시군도 추정)"으로 안내한다 (정보 없음을
  없애기 위한 최후의 폴백)

**5) 검색어 보정**
검색어 자체가 "OOIC", "OOJC"처럼 고속도로 시설명으로 끝나는데(예: "장수IC"), 지오코딩으로
찍힌 핀이 진출입 램프 근처라 우연히 국도가 더 가깝게 잡히는 경우가 있다. 이럴 때는 500m 이내
고속국도 후보가 있으면 그쪽을 우선으로 밀어준다.

**6) 결과 저장**
최종 결과를 화면에 보여주는 동시에, 백그라운드로 Supabase의 `query_logs` 테이블에 기록한다
(자동 테스트 시에는 `nolog=1` 파라미터를 붙여 기록을 생략한다).

### 2-0-1. 요약 다이어그램

위 설명을 압축한 순서도다 (세부 설명은 위 §2-0 참고).

```
사용자 입력 (주소 / IC·JC명 / 핀 이동)
    ↓
[지오코딩] Kakao 주소검색 + 키워드검색 병렬
    ↓ (동명이지 시 { ambiguous: [...] } 응답 → 후보 선택 UI, §15)
핀 좌표 확정
    ↓
[도로 인식 1] highway-grid.json에서 고속도로 탐색 (반경 500m)
    ↓ (200m 이내 발견 시 아래 단계 생략 — 성능)
[도로 인식 2] osm-road-grid.json에서 국도(n)·지방도(p)·주요간선(x) 탐색 (반경 500m)
    ↓
[관리주체 판정] — 상세 우선순위는 §2-1
  · 고속국도: highway-jurisdiction.ts formatAgency() 4단계
  · 그 외: road-rules.ts (행정구역 필요 시 coord2regioncode 호출)
  · 인식된 도로 없음: "OO시 도로관리부서 (시군도 추정)"
    ↓
[검색어 보정] 검색어가 IC/JC/TG 등으로 끝나면 500m 내 고속국도 후보 우선
    ↓
결과 카드 표시 + [백그라운드] Supabase query_logs 저장 (nolog=1 시 생략)
```

**별도 경로 — 노선명+km 검색** (직제 대조·내부 검증용):
"중부선 220"처럼 검색어가 `노선명 숫자` 패턴이면, 위 지오코딩 과정을 통째로 건너뛰고
`findRoutePointByKm()`이 `highway-grid.json`에서 노선명이 부분일치하면서 요청한 km에
가장 가까운 포인트를 바로 찾아 반환한다. 이건 일반 사용자용 기능이 아니라, 직제 표의
경계값(예: "이 지점이 정확히 몇 지사 관할로 바뀌는가")을 눈으로 확인할 때 쓰는 내부 검증
도구다. (요청 km에서 10km 넘게 벗어나면 그런 지점이 없다고 보고 일반 검색으로 넘어간다)

**주요 파일**: `lib/road-analyzer.ts`(도로 탐색), `lib/highway-jurisdiction.ts`(직제·민자 판정),
`lib/road-rules.ts`(도로법 규칙 판정), `app/api/search/route.ts`(위 전체 과정을 묶는 단일 API)

### 2-1. 관리주체 판정 근거자료 — 신뢰도 계층 (2026-08-13 재정의)

판정에 쓰는 표들은 **출처의 신뢰도가 서로 다르다.** 하나는 회사가 공식적으로 배포하는
단일 문서 그대로이고, 나머지는 뉴스·운영사 홈페이지 등을 뒤져서 직접 조사해 만든 보조자료다.
**공식 원문서가 있으면 그걸 최우선으로 따르고, 없을 때만 보조자료로 보완한다**는 게 핵심 원칙이다.

**고속국도 (`highway-jurisdiction.ts: formatAgency`)**

| 순위 | 표 | 근거자료 성격 |
|---|---|---|
| 1 | `JURISDICTION_RULES` (직제) | 회사가 **매년 공식 배포**하는 단일 원문서(직제세부운영계획, hwpx) — 최고 신뢰도 |
| 2 | `PRIVATE_ROAD_CODES` (민자매핑) | CEPHIS(민자도로관리지원센터) 자료·뉴스 등 **개별 조사**로 구축한 보조자료 — 1순위에 없을 때만 적용 |
| 3 | `TRANSFERRED_SECTIONS` (이관) | 개별 조사 보조자료 — 1·2순위 모두 없을 때만 적용 |
| 4 | 폴백 | `"관할 확인 필요 (직제 미기재 구간)"` |

과거엔 민자매핑(2순위)이 1순위였다. 그런데 ETC 좌표코드와 직제 원문(hwpx)의 노선코드가
서로 다른 사례가 발견됐다 — 예를 들어 실제로는 같은 물리적 도로인데, ETC 좌표 데이터는 이걸
"구리포천선"(코드 0291)이라 부르고, 직제 원문은 같은 도로를 "세종포천선"(코드 0290)이라는
다른 코드로 등재해뒀다. 예전 순서(민자매핑 최우선)로는 0291 코드로 조회할 때 직제표(0290)를
아예 들여다보지도 않고 민자 운영사로 확정해버려서, 정작 회사 공식 문서에 이미 관할 지사가
정해져 있는 걸 놓치고 있었다. 그래서 순서를 직제 우선으로 바꿨다 (자세한 경위는 §4-3, 신설
노선 추가 시 이런 코드 불일치를 미리 점검하는 절차는 §6 참고).

**우선순위만 바꾸는 걸로는 부족했다** — `lookupJurisdiction()` 함수 안에 "민자 코드면 직제
조회를 아예 건너뛴다"는 가드 코드가 예전 순서(민자 최우선) 시절부터 남아있었는데, 이게
직제를 최우선으로 보게 바뀐 새 순서와 충돌해서, 0291처럼 민자매핑과 직제 규칙이 둘 다 있는
코드에서 직제 매칭 자체가 무력화되는 버그가 있었다. 이 가드를 제거해서 실제로 고쳤다
(2026-08-13). 우선순위 로직과 개별 조회 함수 양쪽에 같은 전제를 심어두면 이렇게 한쪽만
고치고 다른 쪽을 놓치는 사고가 나기 쉽다는 교훈.

실제로 0291에는 0290과 동일한 직제 규칙(파주지사)을 등록해서 반영했다. 기존 16개 민자코드·
4개 이관구간 중 나머지는 각자의 코드 기준으로 직제 항목과 겹치지 않아 판정 결과가 바뀌지
않았다(전수 대조로 확인 완료). 다만 0291/0290처럼 "코드 번호는 다른데 물리적으로 같은
도로"인 숨은 사례가 더 있을 가능성은, 전체 코드를 좌표 기준으로 대조하기 전까지는 배제할 수 없다.

**고속국도 외 (`road-rules.ts`)**

| 순위 | 판정 | 근거자료 성격 |
|---|---|---|
| 1 | `PRIVATE_LOCAL_ROADS` (민자 지방도·교량 5건, 도로명 매칭) | 개별 조사 — 도로 등급(국도/지방도/간선) 상관없이 최우선 |
| 2a | 국도 → 지방국토관리청 (`RRO_BY_SIDO`) | **도로법 제23조 + 지방국토관리청 관할구역 (아래 출처)** — 신뢰도 높음 |
| 2b | 지방도 → 도(道) (`resolveProvincialRoad`) | **도로법 제23조 원칙 그대로 구현** — 신뢰도 높음 |
| 2c | 주요간선(x) → `URBAN_EXPRESSWAYS`(24건, 개별조사) 매칭, 안 되면 해당 시/군 | 매칭 성공 시만 특정 기관 |
| 3 | 인식된 도로 없음 → `"OO시 도로관리부서 (시군도 추정)"` | 폴백 |

**2a·2b의 출처**: 도로법 제23조는 "일반국도는 국토교통부(산하 지방국토관리청), 지방도는
도지사가 관리청"이라는 **원칙**만 정한 법 조문이다. 실제로 어느 시/도를 어느 지방국토관리청이
담당하는지는 별도 법령인 **"국토교통부와 그 소속기관 직제 시행규칙" [별표 2] 국토관리사무소의
명칭·위치 및 관할 구역**(제22조제1항 관련, 2018.3.30 개정)에 나온다. 이 별표를 직접 확인해서
`RRO_BY_SIDO`를 채웠다. 지방도는 법 조문 원칙(도지사 관할)을 그대로 구현한 것이라 별도 표가 필요 없다.

**시/도 단위 단순화의 예외 한 건**: 별표 원문을 실제로 읽어보면 관할구역이 시/도 단위가 아니라
**읍·면 단위까지 세밀하게** 지정돼 있다. 대부분은 시/도 경계와 지방국토관리청 경계가 일치해서
우리 코드처럼 시/도 단위로 단순화해도 문제가 없지만, **딱 한 곳 예외**를 발견했다: 경상북도는
전역이 부산지방국토관리청 관할인데, **상주시 화북면만 유일하게 대전지방국토관리청**(보은국토관리사무소)
관할로 별도 지정돼 있다. `road-rules.ts`의 `resolveNationalRoad()`에 이 예외를 별도로 반영해뒀다.

고속국도와 달리 국도·지방도는 어느 지방국토관리청·어느 도(道)가 담당인지가 **법령상 고정**돼
있다. 그래서 애초에 "직제표 vs 보조자료가 충돌"할 구조 자체가 없다(안정적). 남은 리스크는
①위 예외처럼 시/도 단위 단순화 과정에서 아직 못 찾은 읍·면 단위 예외가 더 있을 수 있다는 점,
②`PRIVATE_LOCAL_ROADS`·`URBAN_EXPRESSWAYS` 같은 사람이 직접 하나씩 조사해 넣은 하드코딩
목록에서 아직 못 찾은 사례가 누락돼 있을 수 있다는 점이다.

---

## 3. API

### GET `/api/search`

| 파라미터 | 설명 |
|----------|------|
| `query` | 주소, 장소명, 또는 `노선명 km` 패턴(예: `중부선 220`, §2-0-1의 내부 검증 경로) |
| `lat`, `lng` | 좌표 직접 지정 (핀 이동 시) |
| `nolog=1` | 이용 로그 저장 생략 (자동 테스트용) |

응답: `{ lat, lng, placeName?, candidates[], recommendation, altCandidates[] }`
동명 지번(다중 시/도)이 감지되면 위 형식 대신 `{ ambiguous: [{ label, lat, lng }] }`을 반환한다
(선택 가능한 후보 목록 — §2-0·§15 참고, 이 경우 `recommendation`은 없음).

- `recommendation.roadType`: 고속국도 / 일반국도 / 지방도 / 도시고속화도로 / 시군도
- `recommendation.contacts?`: 전북본부 6개 지사 관할일 때만 포함 (§7-1)
- 실측 응답 시간: 좌표 31~48ms, 주소 검색 40~48ms

---

## 4. 데이터 파일

| 파일 | 크기 | 내용 | 출처 |
|------|------|------|------|
| `highway-grid.json` | 9.1MB | 고속도로 중심선 + 시설·램프 앵커 (ETC 코드·노선명·km·좌표, 0.1도 격자) | ETC CSV + 민자 보간 + 시설/램프 보완(§4-1) |
| `highway-nodes.json` | 120KB | IC·JC·SA 노드 (이름·routeNo·km) | 공공데이터포털 |
| `osm-road-grid.json` | 7.2MB | 국도·지방도·주요간선 127,113 포인트 | OpenStreetMap |

각 파일의 점 하나가 어떤 정보를 담고 있는지는 아래와 같다 (키 이름을 줄여서 용량을 아꼈다):

```
highway-grid:  { r: ETC코드, n: 노선명, k: km, a: lat, o: lng }
osm-road-grid: { c: 'n'|'p'|'x', r: 노선번호(0=없음), n: 도로명, a: lat, o: lng }
```

### 4-1. 고속도로 데이터 출처

- **ETC 도로중심선 CSV**: 공공데이터포털에서 받는 "한국도로공사 도로중심선" 원본 자료.
  전국 모든 고속도로의 중심선을 따라 약 100m 간격으로, **노선번호·도로명·이정(km)·위도·경도**를
  한 행씩 담고 있다. 즉 "이 고속도로의 이 지점이 정확히 어느 좌표에 있고, 기점에서 몇 km
  떨어져 있는지"를 알려주는 원본 좌표 데이터다. `highway-grid.json`은 이 CSV를 그대로 우리
  포맷으로 변환한 것이다.
  https://www.data.go.kr/data/15045608/fileData.do
- **노드 CSV**: 공공데이터포털의 "한국도로공사 노선별 노드 이정" 자료. IC(나들목)·JC(분기점)·
  SA(휴게소) 같은 주요 시설의 **이름·소속 노선번호·그 노선의 몇 km 지점인지**를 담고 있다.
  단, 이 자료엔 **좌표(위도·경도)가 들어있지 않다** — 이름과 km 값만 있다. 그래서 실제 좌표가
  필요하면 그 이름을(예: "장수IC") 카카오 지오코딩으로 따로 찾아야 한다. `highway-nodes.json`이
  이 CSV를 그대로 옮긴 파일이고, 앞서 §2-0-1에서 언급한 "노선명+km 검색" 기능이나, 아래
  §6 "민자 신설 노선 추가 절차"에서 이 파일을 활용한다.
  https://www.data.go.kr/data/15064247/fileData.do
- **ETC CSV에 없는 민자 구간**은 위 정부 자료만으로는 좌표를 알 수 없으므로, 두 가지 방식으로 보완한다:
  - **OSM 실도로 형상 추출** (권장, 2026-08-13 도입): Geofabrik(OSM 데이터 배포처)의 모터웨이
    데이터에서 노선명·도로번호로 해당 구간을 필터링 → 실제 IC·JC 좌표를 기준점(앵커)으로 삼아
    Dijkstra 최단경로 알고리즘으로 두 기준점 사이의 실제 도로 형상을 추출 → 기준점 사이 공식
    km 거리 대비 실측 거리 비율이 0.5~2.5배 범위 안이면 채택, 벗어나면 그 구간만 직선 보간으로
    대체 → 0.5km 간격으로 다시 샘플링. 직선 보간보다 실제 도로 곡선에 가까워서, 다리·해안처럼
    굴곡진 구간에서 오매칭이 크게 줄어든다.
    적용 완료: 인천김포선, 익산평택선의지선, 수원광명선, 봉담송산, 용인서울선, 서울문산선,
    영천상주선, 광주원주선, 남해제3지선, 인천대교선, 평택시흥선,
    중앙선(대구부산), 수도권제2순환선(포천~화도), 수도권제2순환선(시화MTV)
    (구리포천지선은 2026-08-13에 OSM 추출본을 폐기하고 ETC CSV 공식 코드 0292로 교체 — 아래 §6 참고)
  - **IC 좌표 직선 보간** (구 방식, OSM에도 도로 형상이 없을 때만 사용): 노드 CSV의 IC 이름을
    지오코딩해 좌표를 구한 뒤, IC와 IC 사이를 그냥 직선으로 0.5km 간격 점을 찍어 잇는다.
    실제 도로가 휘어있으면 오차가 생길 수 있는 임시방편이라, OSM 형상 추출이 가능하면 그쪽을
    우선한다. 잔존 적용 구간: 새만금포항선지선, 서산영덕선 말단, 서천공주선

**톨게이트(TG)·나들목 루프 근접 오탐 패턴**: ETC 도로중심선 CSV는 본선 중심선만 따라가고,
톨게이트 요금소 광장·진출입 램프·인터체인지 루프처럼 본선에서 옆으로 빠진 구조물까지는
좌표를 따로 잡아주지 않는다. 그래서 사용자가 지도에서 이런 시설을 정확히 클릭해도, 가장
가까운 고속도로 중심선 포인트가 300~1500m씩 떨어져 있어 500m 반경 밖으로 밀리거나 국도·
지방도에 순위가 밀리는 오탐이 발생한다 (최초 발견 사례: 순천TG → 국도17호선, 화도TG →
지방도86호선으로 오판정, 2026-08-18).

**전국 일괄 보완 완료** (2026-08-18): 건별 대응 대신 `highway-nodes.json`에 이미 있는 전국
IC·JC·TG·SA **908개 시설**(고유 이름 779개)을 전부 카카오 키워드검색으로 일괄 지오코딩해서
`highway-grid.json`에 "시설 앵커 포인트"로 추가했다.
- **코드 매칭 방법**: 지오코딩 후보 좌표가 있어도 어떤 ETC/커스텀 코드로 태깅해야 할지는
  별도 문제다 — 노드의 `routeNo`는 highway-nodes.json 자체 번호 체계라 highway-grid.json의
  ETC 코드와 직접 대응하지 않는다(§6 세종포천/구리포천 사례처럼 노선명 표기도 소스마다 다름).
  그래서 routeNo 변환 대신, **노드의 `routeName`과 문자열이 일치하는 기존 grid 포인트를
  찾아 그 포인트의 좌표에서 3km 이내인 지오코딩 후보만 채택**하고, 그 기존 포인트의 코드를
  그대로 물려받는 방식을 썼다. (20km로 시작했다가 "여수고가교"가 "제2경인선(인천안양)"으로
  잘못 매칭되는 등 오탐이 섞여 3km로 좁힘 — 최종 결과 중앙값 21m, 최대 1.8km)
- **결과**: 908개 중 757개(83%) 매칭·추가 성공. 나머지는 지오코딩 실패(40, 카카오에 개별
  장소로 없음)·같은 노선명의 grid 포인트가 아예 없음(41, §6 코드 세분화 문제와 동일 유형)·
  3km 이내 매칭 실패(70)로 미반영 — 전부 `highway-nodes.json`의 원본 표기 문제이므로 발견되는
  대로 개별 확인 필요.
- **한계**: 공식 노드 목록에 아예 없는 현지 통칭(예: "화도IC"의 실제 통용명 "마석IC")이나,
  인터체인지 자체가 넓게 퍼진 복합 루프 구조라 앵커 하나로 못 덮는 경우는 이 배치로도 못 잡는다
  (남양주 화도/마석 인터체인지, 기흥동탄IC에서 재발 확인 — 신고 좌표를 직접 앵커로 추가해 해결).

**전국 램프(진출입로) 형상 통째 추가로 근본 해결** (2026-08-20): "시설 앵커 포인트 1개"로는
인터체인지 루프 전체를 못 덮는 근본 원인을 해결하기 위해, 점 하나가 아니라 **각 인터체인지의
실제 진출입 램프 도로 형상 전체**를 추가했다.
- **방법**: Geofabrik OSM 데이터(§4-2와 동일 출처, `motorway_link` 등급 — 고속도로 본선이
  아니라 진출입 램프만 담긴 클래스)에서 전국 11,533개 램프 세그먼트를 추출 → 각 세그먼트의
  중간 지점에서 **1.5km 이내에 있는 기존 `highway-grid.json` 포인트**를 찾아 그 포인트의
  노선 코드를 그대로 물려받음(전국 96개 이상 노선에 이미 데이터가 있어 1.5km 이내 매칭률
  98%로 매우 높음 — §4-1 "전국 일괄 보완"의 3km처럼 오탐 걱정할 필요가 적은 이유는, 여기선
  "노선명 문자열 일치"가 아니라 "물리적으로 가장 가까운 아무 포인트"를 그대로 쓰기 때문에
  애초에 엉뚱한 지역으로 튈 수가 없음) → 150m 간격으로 리샘플해 48,438개 포인트 생성
- **결과**: `highway-grid.json` 포인트 수 61,089 → **109,527개**로 증가 (파일 크기 4.1MB →
  9.1MB). 순천TG(107m→60m)처럼 기존에 고친 지점은 더 정확해졌고, 한 번도 손대지 않았던
  신갈JC 주변도 500m 이상 벗어나던 지점들이 대부분 50~60m대로 들어옴 — 전국 인터체인지에
  일반적으로 적용되는 근본 해결책.
- **성능 영향 없음**: 포인트가 거의 2배로 늘었지만 `highway-grid.json`은 서버 시작 시 한 번만
  읽어 메모리에 캐시하고(§2-0), 검색은 항상 입력 좌표 주변 0.1도 격자 9칸만 훑는 구조라
  (§2-0 "고속도로인지 확인") 늘어난 포인트는 원래도 데이터가 있던 칸이 촘촘해지는 것뿐이라
  요청당 계산량이 유의미하게 늘지 않는다.
- **남은 한계**: 그래도 못 잡는 경우(공식 노드에도 없고 OSM 램프 데이터에도 안 잡히는 아주
  드문 사례)는 여전히 신고 기반 개별 대응(§4-1 대응 절차)이 필요하다.

**시설 앵커를 카카오 지오코딩 → 도로공사 공식 좌표로 교체** (2026-08-29): 위 "전국 일괄 보완"
배치는 시설 이름을 카카오 지도에 검색해서 나온 좌표라, 동명이인 오매칭 위험이 구조적으로
남아있었다(예: "여수고가교"가 19km 떨어진 "제2경인선"으로 잘못 매칭). 한국도로공사가 자체
운영하는 별도 포털 `data.ex.co.kr`("고속도로 공공데이터 포털")에 IC/JC·영업소(TG)·휴게소(SA)
좌표를 직접 제공하는 OpenAPI가 있는 것을 확인하고, 카카오 지오코딩 대신 이 공식 좌표로
교체했다.
- **인증**: `data.ex.co.kr/openapi/apikey/requestKey`에서 계정가입 없이 간단한 설문 후
  즉시 이메일로 키 발급(`.env.local`의 `EX_DATA_API_KEY`). 호출 시 `User-Agent` 헤더가
  없으면 "Request Blocked"로 차단되니 반드시 포함해야 한다.
- **API 3종**: `openapi/locationinfo/locationinfoIc`(출입시설, 659건, 좌표결측 0%),
  `openapi/locationinfo/locationinfoRest`(휴게소, 203건, 좌표결측 0%),
  `openapi/locationinfo/locationinfoUnit`(영업소, 590건, 좌표결측 194건=33% — "동김천(테스트)"
  같은 내부 테스트 레코드·본선영업소 등 톨게이트가 아닌 잡다한 시설이 섞여 있어 결측이 생기며,
  민자/도공 여부와는 무관함을 확인). 결측 194건은 옛 카카오 데이터도 없으므로 그대로 미반영.
- **코드 매칭**: API의 `routeNo`(IC·SA)는 우리 ETC 코드와 대부분 일치하지만 100% 신뢰할 수는
  없다 — 두 코드를 하나로 합쳐 부르는 경우(`0120`=광주대구선+무안광주선), 코드값 자체가
  다른 경우(SA의 `0100`→우리 `0102`, `0652`→`0655`)가 실제로 발견됐다. 영업소(TG)는 애초에
  API가 3자리 별도 체계라 코드가 아예 안 맞는다. 그래서 모든 레코드에 대해 먼저
  ①직접 코드 일치 또는 노선명 완전 일치를 시도하고, 실패하면 ②기존 grid에서 반경 5km 이내
  가장 가까운 점의 코드를 물려받는 방식(전국 램프 형상 배치와 동일 원리)으로 이중 처리했다.
  km 값은 `highway-nodes.json`에서 시설명이 일치하는 항목의 공식 km을 우선 사용하고, 없으면
  반경 15km 이내 같은 코드 포인트의 km을 대신 썼다.
- **결과**: IC 659 + SA 203 + TG(좌표있는 396건 중) = 1,235건을 새 공식 좌표로 채택(23건은
  최근 개통 구간이라 grid에 참조할 기존 코드 포인트가 15km 이내에 없어 km 산정이 안 돼 보류 —
  새만금포항선 포항 연장, 수도권제2순환선 봉담~정남 구간 등). 기존 카카오 배치 포인트 중
  ETC 정식 코드로 태깅된 683개(9xxx 커스텀 코드 제외 — 9106 제2경인연결고속도로, 9403
  화도/마석 인터체인지처럼 공식 API에도 없는 수작업 보간 구간은 그대로 유지)를 제거하고 교체.
  회귀 테스트(기흥동탄IC·순천TG·안양과천TG·경부선 392.7km 수원지사 등) 통과.
- **여전히 대체되지 않는 것**: 도로 자체의 선형(도로중심선 CSV·노드 이정)은 이 API들로 대체
  안 된다 — `공간정보노선현황` API(`openapi/roadEtcInfo/spinRouteList`)가 있지만 노선의
  시작·종점 km과 총연장만 주지 중간 좌표는 안 준다. 새 구간이 개통되면 여전히 위 "ETC 도로중심선
  CSV" 재다운로드가 필요하다. 관할청(직제) 정보도 이 포털 어디에도 없어 §16 "직제 개편 시" 절차는
  그대로 유지된다.

**민자 고속도로 17개 노선을 국토교통부 표준노드링크로 교체** (2026-08-29): 민자 고속도로는
ETC CSV에 애초에 없어서(도로공사 직영이 아니므로) OSM 실도로 추출이나 IC 좌표 직선보간(§4-1
앞부분)으로 대체해왔는데, 이 방식들은 결국 "추정값"이라 오탐 위험이 있었다. 국토교통부가
운영하는 `국가교통정보센터(ITS, its.go.kr)`의 **표준노드링크**가 도공/민자 구분 없이 전국
고속도로를 공식 실측 좌표로 통합 관리하는 것을 확인하고, 민자 구간만 이 자료로 교체했다
(도공 직영 구간은 이미 정확한 ETC CSV가 있으므로 그대로 둠 — 안 아픈 곳은 안 건드림).
- **다운로드**: `its.go.kr/nodelink/nodelinkRef`에서 로그인 없이 SHP 파일 다운로드 가능
  (실제 다운로드 URL은 `its.go.kr/opendata/nodelinkFileSDownload/{atchFileId}/0` 패턴이며,
  파일 목록은 `POST its.go.kr/opendata/getNodeLinkDataFileList`로 조회— 사이트 UI에는
  일반적인 `<a href>` 링크가 아니라 JS로 그려지는 커스텀 엘리먼트라 브라우저 자동화로는
  링크를 못 찾고, 페이지 스크립트를 직접 읽어야 찾을 수 있었다). 약 2~4주 간격으로 갱신되며
  이번엔 2026-08-12자 스냅샷(257MB) 사용.
- **좌표계 변환 필요**: SHP 좌표는 TM 중부원점(미터 단위, `+proj=tmerc +lat_0=38 +lon_0=127
  +k=1 +x_0=200000 +y_0=600000 +ellps=GRS80`)이라 위경도(WGS84)로 재투영해야 한다(pyproj).
- **"이정"(km) 필드가 없음**: 도로중심선 CSV와 달리 표준노드링크의 링크(LINK) 테이블엔
  구간 자체의 길이(`LENGTH`, m)만 있지 기점부터 누적 거리는 없다 — F_NODE/T_NODE로 이어진
  구간들을 순서대로 이어붙여 직접 누적 계산해야 하는데, 전국 63개 노선 전체에 이 계산을
  적용하면 오차가 누적될 위험이 있어 민자 17개 노선만으로 범위를 좁혔다. km 값은 새로
  계산하지 않고, 각 신규 좌표점에서 **기존(교체 전) 같은 코드의 포인트 중 가장 가까운 점의
  km을 그대로 물려받는 방식**(반경 6km 이내)을 썼다 — 좌표 정밀도는 크게 좋아지고 km은
  기존 근사치를 유지하는 절충.
- **이름·번호 불일치**: 국토부 `ROAD_NO`(도로번호)는 우리 ETC 코드와 전혀 다른 체계이고
  **같은 번호를 서로 다른 여러 노선이 공유**한다(예: `ROAD_NO=1`이 경부·함양울산·중앙 등
  7개 노선에 쓰임) — 그래서 번호는 버리고 `ROAD_NAME`(예: "세종포천고속도로")을 정규화해서
  ("~선"↔"~고속도로" 접미사 치환) 우리 노선명과 매칭했다. 일부는 순서가 뒤바뀌어 있거나
  (영천상주선 ↔ MOLIT "상주영천고속도로") 표기가 달라(남해제3지선 ↔ "남해고속도로제3지선")
  수동 보정이 필요했다.
- **이름이 겹치는 노선**: 수도권제2순환선의 봉담송산(400)·시화MTV(9601)·포천화도(9403)·
  인천김포선(4001) 4개 구간은 국토부 자료에서 전부 "수도권제2순환고속도로" 하나의 이름을
  쓴다(물리적으로 끊어진 별개 구간인데 이름만 같음) — 그래서 각 링크의 중간좌표가 네 구간
  중 어느 기존 좌표에 가장 가까운지로 자동 분류했다. 제2경인고속도로도 마찬가지로 본선
  (1101)·제2경인연결고속도로(9106)·인천대교선(1102) 세 코드가 이름을 공유해 같은 방식으로
  분류했다.
- **처음엔 "국토부 자료에 없다"고 잘못 판단했던 3개 노선**: 익산평택선의지선(173)·
  인천김포선(4001)·인천대교선(1102)은 우리 노선명을 그대로 정규화해서 찾을 땐 안 나왔지만,
  기존 좌표 주변을 반경 검색해보니 실제로는 **다른 이름 아래 존재**했다 — 173은 운영사명과
  같은 "서부내륙고속도로", 4001은 "수도권제2순환고속도로"(위 4-way 분류로 편입), 1102는
  "제2경인고속도로"(위 3-way 분류로 편입)로 잡혔다. 그래서 노선명 매칭이 실패하면 바로
  "국토부에 없다"고 단정하지 말고, 기존 좌표 주변 반경 검색으로 실제 존재 여부부터 확인해야
  한다는 교훈을 얻었다.
- **결과**: 최종 19개 민자 노선 전부(173·1102·4001 포함) 국토부 좌표로 교체 완료 —
  기존 포인트 10,486개를 제거하고 23,097개로 교체(밀도 약 2.2배 증가, 링크당 100m
  간격으로 리샘플). 9106(제2경인연결고속도로)처럼 13개 포인트짜리 성긴 수작업 보간
  구간도 591개의 촘촘한 실측 좌표로 대체됨. 회귀 테스트(9106 안양과천TG, 1102 인천대교선,
  173 익산평택선의지선, 4001 인천김포선, 9153 평택시흥선, 경부선 392.7km 수원지사 등)
  전부 통과, 좌표 전부 대한민국 영역 내 확인.
- **분류 기준점이 성기면 오분류가 생김**: 1102(인천대교선)를 1101/9106과 지리적으로
  분류할 때 처음엔 "교체 전 구자료"(1102는 옛 OSM 배치 때 km 18.8에서 끊긴 176개 점)를
  기준점으로 썼는데, 다리가 육지에 닿는 접속부(km 18.8 이후)는 1102 기준점이 없다 보니
  더 멀리 있는(695m) 1101의 기존 점에 어부지리로 흡수됐다(실사용자가 조회 기록에서
  "여기 인천대교(주)가 맞다"고 지적해 발견). 새로 확보한 1102 좌표(695개, 훨씬 넓게 뻗음)를
  기준점 삼아 재분류하니 정상적으로 1102로 잡혔다 — **지리적 분류는 기준점 데이터의
  범위 안에서만 정확하고, 기준점이 어디서 끊기는지 먼저 확인해야 함**.
- **9106↔수도권제1순환선(1000) 경계 공백 보강** (같은 날 추가 발견): 9106이 끝나는
  지점(km 5.516, 동쪽 경계)과 1000의 기존 좌표 사이에 400m~1.4km 폭의 진짜 좌표 공백이
  있어 그 사이를 지나면 시군도 폴백으로 빠졌다(실사용자가 이 지점도 조회 기록에서 발견 —
  "여기는 수도권제1순환선이 맞다"). 이건 민자 노선 교체와 무관하게 원래부터 있던, 더 오래된
  결함이었다. 공백 좌표 주변(반경 2km) MOLIT 링크를 "제2경인고속도로"/"수도권제1순환고속도로"
  이름으로 걸러 기존 9106/1000 좌표 중 더 가까운 쪽으로 분류해 297개 포인트를 순수 추가만
  했다(기존 데이터 삭제 없음). 재검증 통과.
- **검증 시 주의**: `highway-grid.json`은 dev 서버가 첫 요청 때 메모리에 캐시하고
  프로세스 재시작 전까지 유지한다(§2-0-1). 그래서 서버를 계속 띄워둔 채로 파일을 여러
  번 고쳐 쓰면, 이후 회귀 테스트가 새 파일이 아니라 캐시된 옛 데이터로 통과해버려
  "고쳤는데 검증도 통과했다"는 착시가 생길 수 있다 — 데이터 파일을 바꾼 뒤에는 반드시
  dev 서버를 재시작하고 나서 검증해야 한다(이번 배치 작업 중 실제로 한 번 이 착시를
  겪고 재시작 후 재검증함).
- **의도적으로 손대지 않은 부분**: 도시고속화도로·국도·지방도·시군도(표준노드링크의
  ROAD_RANK 102~107)는 손대지 않았다 — 이 도로들의 관할 판정은 노선/km이 아니라 지역
  (시도·시군구) 기준이라(§5) 정밀 도로 형상의 필요성이 고속도로보다 낮고, 지금 딱히
  문제로 지적된 적도 없어 범위 밖으로 뒀다.

### 4-2. OSM 데이터 (국도·지방도·간선)

- **다운로드**: https://download.geofabrik.de/asia/south-korea-latest-free.shp.zip (로그인 불필요, 544MB) —
  OpenStreetMap의 한국 전체 도로·건물 등 지리 데이터를 통째로 담은 압축 파일이다.
- **추출 규칙**: 이 중 도로 데이터(`gis_osm_roads_free_1.shp`)에서, 도로 등급(`fclass`)과
  도로 번호(`ref`)를 보고 아래 규칙으로 분류한다:

| OSM 조건 | 분류 |
|----------|------|
| trunk/primary + ref 1~99 | `n` 일반국도 |
| trunk/primary/secondary + ref 100+ | `p` 지방도 (secondary의 2자리 ref는 국가지원지방도) |
| trunk + ref 없음 + 이름 있음 | `x` 주요간선 (분당내곡로, 올림픽대로 등) |
| motorway + ref | 제외 (고속도로는 highway-grid가 담당하므로 여기서 뺀다) |

- 변환: Python 스크립트로 0.5km 간격 샘플링 → 0.1도 격자로 인덱싱 (highway-grid와 동일한 방식)
- 갱신: OSM 데이터는 전 세계 자원봉사자들이 매일 갱신하므로, 필요하면 언제든 재다운로드해서
  재변환할 수 있다(완전 자동화 가능한 절차 — §16 참고).

### 4-3. 직제 관할 테이블

- **출처**: 한국도로공사 2026년 직제세부운영계획 — 본사 미래전략처가 연 1회 배포하는
  내부 공식 문서. "어느 지역본부·어느 지사가 어느 고속도로의 몇 km부터 몇 km까지 담당하는가"를
  정해놓은, 회사 조직표의 도로 버전이라고 보면 된다. 원본 파일 형식은 hwpx(한글 문서 파일).
- **파일**: `lib/highway-jurisdiction.ts`의 `JURISDICTION_RULES` 배열에 이 문서 내용을 옮겨
  담았다. 항목 하나는 `{ etcCode, kmStart, kmEnd, hq, branch }`(ETC코드, 시작km, 끝km,
  지역본부, 지사) 형태다.
- **원문 대조 방법**: hwpx 파일은 사실 zip 압축 포맷이라, 압축을 풀면 안에 XML 파일들이
  들어있다. 그중 `Contents/section*.xml`에서 `<hp:t>`라는 태그(한글 문서의 텍스트 조각을
  감싸는 태그) 안의 텍스트를 전부 뽑아내면 원본 표를 그대로 복원할 수 있다. 이렇게 복원한
  표를 `JURISDICTION_RULES`와 한 줄씩 대조해서 옮겨 담았다.
- **km 축 불일치 주의**: 직제 문서가 표시하는 구간과 ETC 좌표 데이터가 표시하는 구간이
  기점(0km로 잡는 지점)을 다르게 잡는 경우가 있다. 발견된 사례:
  - 완주장수선(코드 0205) 0~24.49km는 새만금포항선의지선(코드 2040)과 물리적으로 완전히
    같은 도로다 — 이런 경우 양쪽 코드 모두에 똑같은 관할 규칙을 등록해둬야 어느 코드로
    조회해도 정확한 답이 나온다.
  - 당진청주선(코드 0320): 직제 문서는 아산을 기점(0~20.98km)으로 잡는데, ETC 좌표 데이터는
    당진을 기점(23.0~44.1km)으로 잡는다 — 두 기점 사이 거리만큼 오프셋을 더해서 맞춰줬다.
- **타기관 이관 구간**: `TRANSFERRED_SECTIONS` 배열(`{ etcCode, kmStart, kmEnd, agency }`)은
  직제 표의 관할 범위 밖이지만, 실제로는 다른 기관·민자회사로 넘어간 것으로 확인된 구간을
  별도로 관리하는 표다. 이렇게 따로 관리하지 않으면, 직제 표에 없는 구간을 전부 "도공 아님"
  으로만 처리하게 돼서 정확한 이관처를 안내할 기회를 놓친다.
  - 예: 경부선 416.05~426km(한남~양재 구간, 2002년 서울시로 이관), 중앙선 10.12~108.58km
    (대구~부산 구간, 신대구부산고속도로(주)라는 민자회사가 운영), 경인선 0~28.5km
    (인천대로·국회대로로 이름이 바뀌면서 일반도로화)
- **직제 미기재 폴백**: `JURISDICTION_RULES`·`TRANSFERRED_SECTIONS` 어느 표에도 없는 구간은
  `formatAgency()`가 `"관할 확인 필요 (IC명 인근, 직제 미기재 구간)"`을 반환한다.
  **회사 공식 문서에 없는 구간을 함부로 "도공 관할"이라고 단정하지 않는 것이 원칙**이다.
  단, 가장 가까운 후보가 미기재 구간이라도 +150m 이내에 관할이 확정된 다른 후보가 있으면
  (예: 두 노선이 물리적으로 겹쳐 지나가는 구간에서 확정 노선 쪽이 근처에 있는 경우)
  그쪽을 대신 추천한다.

---

## 5. 관리주체 판정 테이블 (`lib/road-rules.ts`)

고속국도가 아닌 도로(국도·지방도·간선)의 관리주체를 정하는 표들이다. §2-1에서 설명했듯
이쪽은 법령(도로법 제23조)에 근거한 시/도 단위 고정 매핑이 기본이라 비교적 안정적이다.

### 지방국토관리청 관할 (일반국도, 도 지역)
서울·인천·경기→서울청 / 강원→원주청 / 대전·세종·충북·충남→대전청 /
전북·광주·전남→익산청 / 부산·대구·울산·경북·경남→부산청 / 제주→제주특별자치도

> 행정구역 통합 대응: 카카오가 반환하는 신명칭(예: 전남광주통합특별시)도 매핑에 포함.
> 대도시 판정은 정규식이 아닌 **명시 목록**(METRO_SIDOS) 사용 — 통합시 오매칭 방지.

### 도시고속화도로 테이블 (URBAN_EXPRESSWAYS, 시도|도로명 키)
이름은 있지만 도로 번호가 없는 도로 중, 사실상 고속도로처럼 운영되는 "도시고속화도로"를
별도로 관리하는 목록이다. 위 지방국토관리청 매핑과 달리 이건 도로 하나하나를 개별 조사해
채운 것(§2-1의 "보조자료" 계열)이라 24개 목록에 없는 도로는 그냥 일반 지자체 관할로
처리된다. 올림픽대로·강변북로·내부순환로·동부간선로 등 → 서울시설공단 /
분당내곡로·분당수서로 → 서울시·성남시 / 신천대로 → 대구 / 동서고가로·번영로 → 부산시설공단 등 24개

### 민자 지방도·교량 테이블 (PRIVATE_LOCAL_ROADS, 도로명 키)
제3경인고속화도로, 일산대교, 미시령터널, 마창대교, 거가대로 — 이 5개도 위와 마찬가지로
개별 조사로 채운 목록이라, 여기 없는 민자 지방도·교량이 있으면 놓칠 수 있다.

### 제주 특례
제주시·서귀포시는 행정시(자치권 없음) → 국도·지방도 모두 제주특별자치도가 관리

---

## 6. 민자 고속도로 (`lib/highway-jurisdiction.ts` → PRIVATE_ROAD_CODES)

민자(민간자본으로 지은 유료) 고속도로 21개 코드가 등록돼 있다. 이 중 5개(0171, 0252, 0291,
1300, 1711)는 ETC CSV에 원래 등재돼 있는 코드고, 나머지 16개(4001, 173, 17, 400, 9171,
9017, 9301, 9052, 9105, 1102, 9153, 9601, 9055, 9403, 9106)는 ETC CSV에 없어서 우리가
직접 좌표를 만들어 부여한 코드다 — 후자 중 어느 것이 OSM 실도로 형상으로 개선됐는지는
§4-1에 정리돼 있다. 직제 표엔 없지만 이관·민자로 확인된 구간은 이 표가 아니라
`TRANSFERRED_SECTIONS`(§4-3)에 별도 등록한다.

`9106`(제2경인연결고속도로, 제이경인연결고속도로(주))은 2026-08-21에 추가됐다. 이름이 비슷한
"제2경인고속도로"(코드 1101, 인천-삼막, KEC 직제상 시흥지사 관할)와는 **완전히 다른 회사가
운영하는 별개의 도로**다(위키백과로 운영사 확인) — 삼막IC에서 이어져 안양시 관양동(안양과천TG)을
지나 안양성남고속도로 방향으로 연결되는 5.5km 짜리 커넥터 도로로, 2017년 개통. ETC CSV·직제
어디에도 없어 삼막IC와 안양과천TG 두 지점을 지오코딩해 직선 보간하고, 확인된 운영사를
`PRIVATE_ROAD_CODES`에 바로 등록했다.

**주의**: 0291처럼 ETC CSV 코드가 있으면서 동시에 민자매핑(`PRIVATE_ROAD_CODES`)에도
등록된 경우, §2-1의 우선순위대로 직제(`JURISDICTION_RULES`)에 그 코드의 규칙이 있으면
그쪽이 이긴다 — 민자매핑은 "직제에 없을 때만" 쓰는 보조 규칙이라는 걸 잊지 말 것.

### 민자 신설 노선 추가 절차
1. `highway-nodes.json`에서 해당 노선의 IC·JC 이름과 각각의 공식 km 값을 확인한다
2. 로컬 API(`/api/search?query={IC명}&nolog=1`)로 그 IC·JC 이름들의 실제 좌표를 지오코딩한다
   (동명이인 주의 — 같은 이름의 IC가 다른 시/도에 있을 수 있어 구분해야 함)
3. Geofabrik OSM 모터웨이 데이터에서 해당 노선명·도로번호로 그래프를 구성한 뒤, 위에서 구한
   좌표들을 기준점(앵커) 삼아 Dijkstra 최단경로로 실제 도로 형상을 추출한다 (§4-1) → 비율
   검증에 실패한 구간만 직선 보간으로 대체 → 0.5km 간격으로 리샘플 → `highway-grid.json`에
   병합한다(새 고유 코드 부여). OSM에 아예 도로 형상이 없으면 전 구간을 직선 보간으로 대체한다
4. `PRIVATE_ROAD_CODES`에 `'코드': '운영사명'` 한 줄을 추가한다
5. 로컬 서버에서 IC 이름 검색과 기존 노선 회귀 테스트를 해보고 이상 없으면 `git push`로 배포한다

### 노선명 병기 (`ROUTE_DISPLAY_ALIAS`)
ETC 도로중심선 CSV가 붙인 노선명이, 카카오맵 등 일반적으로 통용되는 공식 노선명과 다른
경우가 있다. 이럴 때 민원 담당자가 "이게 같은 도로 맞나?" 헷갈릴 수 있으므로,
`lib/highway-jurisdiction.ts`의 `ROUTE_DISPLAY_ALIAS`(ETC코드 → 공식명 매핑)에 등록해두면
화면에 "ETC명(공식명) N.Nkm 지점" 형식으로 두 이름을 같이 보여준다 (`road-analyzer.ts`의
`routeName`을 만드는 부분에서 적용됨).
- 등록 예: `'0291': '세종포천고속도로'` — ETC 데이터가 붙인 이름은 "구리포천선"(실제 개통된
  구간만 가리키는 이름)인데, 카카오맵 등에서는 이 도로 전체의 공식 노선번호명인
  "세종포천고속도로"로 표시된다.
- 화면 구현 메모: 이렇게 병기하면 노선명 문자열에 괄호가 두 개 생긴다("구리포천선(세종포천
  고속도로) 1.1km"). 화면(`app/page.tsx`, `app/stats/page.tsx`)에서 이 문자열 중 km 부분만
  뽑아 "N.Nkm 지점"으로 다시 꾸며주는 정규식이 있는데, 이 정규식은 **문자열 맨 끝의 괄호만**
  km 괄호로 인식하도록 만들어져 있어서, 병기용 괄호가 앞에 추가돼도 표시가 깨지지 않는다.

### 참고: ETC CSV의 세분화된 코드 체계 — 9402(구리포천지선) 사례
ETC 도로중심선 CSV는 하나의 실제 도로를 여러 코드로 쪼개서 관리하는 경우가 있다. 예를 들어
"세종에서 포천까지"라는 하나의 큰 도로 계획을, CSV에서는 0290(세종포천선 전체 노선,
723행)·0291(구리포천선, 실제 개통 구간)·0292(양주지선, 소흘~양주 구간, 68행) 이렇게
세 코드로 나눠서 각각 별도의 좌표 데이터를 갖고 있다.

이걸 몰랐던 탓에 실제로 사고가 있었다: §4-1의 15개 OSM 추출 작업을 할 때 "구리포천지선"
(소흘~양주 구간)을 ETC CSV에 없는 줄 알고 커스텀 코드 `9402`를 만들어 OSM에서 직접 도로
형상을 추출했는데, 사실은 이미 ETC CSV에 **공식 코드 0292**로 정확한 좌표(68행, 0~5.9km)와
직제 규칙(파주지사)까지 다 등록돼 있었다. 나중에 사용자가 "파주지사가 이 구간도 관리한다"고
알려줘서 발견했고, 2026-08-13에 `9402` 그리드 데이터와 민자매핑을 전부 삭제하고 ETC CSV의
`0292` 공식 데이터로 교체했다 (`PRIVATE_ROAD_CODES`에서도 제거 — 이제 직제 규칙만으로 파주지사가
확정된다). 단, 직제 표는 0292가 0~26.11km(양주~법원)까지라고 하는데 ETC CSV 좌표는 5.9km까지만
있어서, 6~26.11km(양주~법원) 구간은 아직 좌표 데이터를 확보하지 못해 반영이 안 된 상태다.

그래서 **새 민자 노선을 위 "민자 신설 노선 추가 절차"로 보간·OSM 추출하기 전에, 먼저 ETC
CSV에 관련 코드가 이미 있는지부터 확인해야 한다** (`awk -F',' '$1=="코드"'`로 CSV를 직접
열어 확인). 이미 정부 공식 좌표 데이터가 존재한다면, 우리가 새로 보간하거나 OSM에서 추출하는
것보다 그 공식 데이터를 그대로 쓰는 편이 훨씬 정확하다.

---

## 7. 클라이언트 UI

| 항목 | 구현 |
|------|------|
| 지도 SDK 로드 | layout에서 `async` + `autoload=false`, 컴포넌트에서 `kakao.maps.load()` 대기 (레이스 방지) |
| 위치 보정 (PC) | 마커 드래그 + 우클릭 |
| 위치 보정 (모바일) | **중앙 고정핀** — 지도를 움직여 지정, `pointer: coarse`로 자동 감지, `?touch=1`로 강제 |
| 도로유형 배지 | 고속국도(녹) 일반국도(청) 지방도(보라) 도시고속화도로(주황) 시군도(회색) |
| 이격 경고 | 핀 위치와 실제 도로 중심선 사이 거리가 100m를 넘으면 화면에 경고 표시 (§2-1에서 설명한 오차 가능성을 사용자에게도 알려주는 장치) |
| 핀 이동 시 검색어 초기화 | `handlePinMove`에서 `setAddress('')` — 이전 검색어가 남아 새 핀 위치와 불일치해 보이는 혼동 방지 |

### 7-1. 지사 연락처 (`lib/branch-contacts.ts`)

`getBranchContacts(branch: string): BranchContact[] | null` — 정적 조회, 외부 API 의존 없음.
현재 **전주·부안·무주·논산·진안·보령지사 6곳만** 등록되어 있고, 그 외 모든 지사는 `null`을 반환해
결과 카드에 연락처 블록 자체가 렌더링되지 않는다 (`lib/road-analyzer.ts`의 `findNearbyHighways()`에서
`lookupJurisdiction()` 결과의 `branch`로 조회 → `RoadCandidate.contacts`에 실어 전달).

```
톨게이트·휴게소   내선 850-XXXX / 외부 063-714-XXXX
교통사고·도로포장  내선 850-XXXX / 외부 063-714-XXXX (뒤 4자리 동일)
```

타 본부로 확대할 때는 해당 본부 연락처를 검증 후 `BRANCH_CONTACTS`에 항목만 추가하면 된다.

---

## 8. 오류 신고 자동 첨부

결과 카드의 "⚠️ 이 결과가 잘못됐나요?" 버튼(또는 결과없음 화면의 신고 링크)을 누르면,
방금 조회했던 검색 정보(검색어·좌표·판정 결과)가 매번 새로 입력할 필요 없이 자동으로
오류 신고 게시판(`/feedback`)의 글쓰기 화면에 첨부된다. 이 정보는 URL 쿼리 파라미터로
전달된다:

| 파라미터 | 내용 |
|----------|------|
| `reportQuery` | 마지막 검색어 (주소 검색 시에만) |
| `reportLat`, `reportLng` | 판정된 좌표 |
| `reportRoadType`, `reportRouteName`, `reportAgency` | 판정 결과 |

`/feedback` 페이지는 이 파라미터를 읽어 글쓰기 폼 위에 미리보기로 표시하고(첨부 해제 가능),
게시 시 `posts` 테이블의 `report_*` 컬럼에 저장한다. 게시글 목록에서는 저장된 좌표로
`/?lat=&lng=` 딥링크("🗺️ 지도에서 위치 확인 →")를 제공해, 관리자가 신고 글만 보고도
당시 상황을 바로 재현해볼 수 있다. `app/page.tsx`는 이 딥링크로 진입하면
`window.location.search`에서 `lat`/`lng`를 읽어 자동으로 그 위치를 검색한다.

---

## 9. 관리자 허브 (`/admin`)

메인 헤더의 "⚙️ 관리자" 버튼이 연결되는 페이지. 카드 2개로 `/feedback`(오류 신고 게시판)과
`/stats`(이용 현황 통계)에 진입한다. 개별 결과에 대한 신고는 결과 카드 버튼이 전담하므로
헤더 버튼과 역할이 분리되어 있다. `/feedback`, `/stats` 로그인 후 화면에는 "관리자 홈" 상호
이동 링크가 있다.

---

## 10. 외부 API 키 관리

| 키 | 용도 | 환경변수 |
|----|------|----------|
| Kakao JavaScript SDK | 지도 표시 | `NEXT_PUBLIC_KAKAO_MAP_KEY` |
| Kakao REST API | 주소·키워드·행정구역 | `KAKAO_REST_API_KEY` |
| Supabase URL / Anon Key | DB (일반 읽기·쓰기용, 삭제 등 민감한 작업은 불가) | `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY` |
| Supabase Service Role Key | DB 관리 작업용(예: 로그 일괄 삭제) — 앱 코드에서는 사용하지 않고, 필요할 때만 로컬에서 직접 사용 | `SUPABASE_SERVICE_ROLE_KEY` |

- Vercel 환경변수 및 `.env.local` 양쪽에 동일하게 설정 (Service Role Key는 `.env.local`에만 —
  절대 커밋하지 않도록 `.gitignore`에 `.env*`로 이미 제외돼 있음)
- Kakao 앱 설정 → 플랫폼 → JS SDK 도메인에 `complaint-map.vercel.app` 등록 필요

---

## 11. Supabase DB 스키마

이 앱은 세 개의 테이블을 쓴다: 오류 신고 글을 담는 `posts`와 그 댓글을 담는 `comments`,
그리고 검색이 있을 때마다 자동으로 한 줄씩 쌓이는 이용 로그 `query_logs`다. 정확한 컬럼
구성은 아래 SQL과 같다.

```sql
-- 오류 신고 게시판
CREATE TABLE posts (
  id bigint PRIMARY KEY GENERATED ALWAYS AS IDENTITY,
  nickname text,
  content text,
  likes int DEFAULT 0,
  created_at timestamptz DEFAULT now(),
  -- 결과 카드 "이 결과가 잘못됐나요?" 클릭 시 자동 첨부되는 검색 컨텍스트 (§8)
  report_query text,
  report_lat double precision,
  report_lng double precision,
  report_road_type text,
  report_route_name text,
  report_agency text
);

CREATE TABLE comments (
  id bigint PRIMARY KEY GENERATED ALWAYS AS IDENTITY,
  post_id bigint REFERENCES posts(id) ON DELETE CASCADE,
  nickname text,
  content text,
  created_at timestamptz DEFAULT now()
);

-- 이용 현황 로그 (검색마다 백그라운드 저장, nolog=1 시 생략)
CREATE TABLE query_logs (
  id bigint PRIMARY KEY GENERATED ALWAYS AS IDENTITY,
  queried_at timestamptz DEFAULT now(),
  input_address text,
  lat double precision,
  lng double precision,
  result_agency text,
  result_agency_full text,
  result_road_type text,      -- 고속국도/일반국도/지방도/도시고속화도로/시군도
  result_route_name text,
  result_distance_m double precision,
  confidence text,            -- 높음/보통/낮음
  found boolean DEFAULT true
);
```

RLS(Row Level Security) 정책: 익명 사용자의 SELECT(조회)·INSERT(등록)만 허용하고,
UPDATE·DELETE는 허용하지 않는다. 그래서 게시판·로그 모두 로그인 없이 쓰고 읽을 수 있지만,
로그를 일괄 삭제하는 것 같은 작업은 일반 앱 키(Anon Key)로는 안 되고 Service Role Key가
있어야 한다 (§10).

---

## 12. 페이지 구조

| URL | 설명 | 접근 제한 |
|-----|------|-----------|
| `/` | 메인 앱 (지도 + 관할 검색) — `?lat=&lng=` 딥링크, `?touch=1` 모바일 모드 강제 지원 | 없음 |
| `/admin` | 관리자 허브 (신고 게시판·통계 진입 카드) | 없음 (진입만, 각 페이지가 자체 게이트) |
| `/feedback` | 오류 신고 게시판 | 없음 (삭제만 비밀번호) |
| `/stats` | 이용 현황 통계 | 관리자 비밀번호 |

---

## 13. 자동 테스트

`/api/search?...&nolog=1`로 호출해서(이용 통계를 오염시키지 않고) 아래 네 가지를 검증한다:

1. **좌표 샘플**: 각 그리드 격자에서 무작위로 좌표를 뽑아 → 같은 노선이 후보로 제대로 반환되는지 확인
2. **IC/JC 이름 검색**: 노드 이름으로 검색했을 때 → 지오코딩부터 관할 판정까지 전체 파이프라인이 정상 동작해 고속국도로 판정되는지 확인
3. **전국 무작위 좌표**: 전국 아무 데나 찍어도 관리주체가 비어있지 않은지 확인 (폴백이 제대로 동작하는지 검증)
4. **주소 검색**: 대표 주소 8건으로 지오코딩→행정구역 재사용 경로가 정상 동작하는지 확인

최종 결과: 318/318 통과, 전국 재검증 310/310 통과 (2026.7 기준, 제주 제외 새 시드).
고속도로 판정은 프로세스 개편(§2-1의 우선순위 변경 등) 전후로 결과가 똑같이 나오는 것을
회귀 테스트로 보장하고 있다. 직제 원문 대조 방법은 §4-3 참고.

---

## 14. 이용 현황 통계 (`/stats`)

**통계 요약 탭**: "총 조회 수"·"결과 있음"·"결과 없음" 세 숫자는 `query_logs` 테이블 전체에
대한 Supabase **exact count 쿼리**(정확한 행 개수를 세는 쿼리)로 별도 조회한다. (2026-08-13
수정 — 이전에는 화면에 표로 보여주려고 가져온 최근 500건짜리 배열의 길이를 그냥 총계로
써버려서, 실제 누적 조회 수가 500건을 넘으면 항상 500에 고정 표시되는 버그가 있었다.)
일별/시간대별/기관 TOP10 차트는 매번 전체 데이터를 다 훑으면 느리므로 성능상 최근 500건
샘플만 그대로 사용하고, 화면에 "최근 500건 샘플 기준"이라는 캡션을 붙여 전체 통계가 아님을
명시한다.

### 14-1. 조회 기록 상세보기 (`조회 기록` 탭)

PC 전용 좌측 목록 / 우측 상세 2단 레이아웃. 목록에서 행을 클릭하면, 그 검색이 실행됐을 당시
`query_logs`에 **저장해둔 값 그대로**로 지도와 결과 카드를 다시 그려서 우측 패널에 보여준다.
(현재 좌표로 다시 조회하는 게 아니다 — 그 사이에 판정 로직이 바뀌었으면 지금 다시 조회했을
때의 결과가 달라질 수 있으므로, "그때 사용자가 실제로 봤던 화면"을 그대로 재현하는 게 목적이다)

- 지도: `KakaoMap` 컴포넌트에 로그의 `lat`/`lng`를 그대로 전달한다. 이 컴포넌트는
  `absolute inset-0`(부모 크기에 꽉 채워 겹쳐 그리는 CSS)로 렌더링되므로, **부모 컨테이너에
  반드시 `relative` + 명시적 높이가 지정돼 있어야 한다** (그렇지 않으면 지도가 모달·패널
  전체를 뒤덮어버리는 버그가 생긴다 — 실제로 겪었던 이슈라 기록해둔다)
- 결과 카드: 메인 화면(`app/page.tsx`)의 결과 카드와 똑같은 스타일(배지 색상, 이격 경고,
  신뢰도 배지)을 그대로 재사용해서, "이용자가 실제로 본 화면과 동일하게 보여준다"는
  요구사항을 충족한다
- 입력값 표시: `input_address`가 `null`인 로그(주소 검색이 아니라 지도에서 핀을 직접 옮겨서
  조회한 경우)는 목록·상세 패널 모두 **"핀 이동"**이라고 이탤릭 회색 글씨로 표시한다
- 판정 근거 문구(`reason`, 예: "민원 위치에서 44m 거리의...")는 로그에 저장하지 않는다 —
  모니터링 목적에는 기관·노선·거리·신뢰도 정보만으로 충분하다고 판단했다

## 15. 동일 지번 다중 후보 처리

§2-0에서 언급한 "동명이지" 상황을 실제로 어떻게 감지하는지 설명한다. `app/api/search/route.ts`의
`geocode()` 함수가 카카오 주소 검색 응답(`documents[]`, 검색 결과 목록)을 받으면, 그 안에서
**서로 다른 `region_1depth_name`(시/도 이름)이 2개 이상** 섞여 있는지를 검사한다. 예를 들어
"산 57-4"로 검색했는데 결과 목록에 "경기도 안성시..."와 "경상남도 통영시..."가 둘 다 들어있으면,
이건 같은 지번이 서로 다른 지역에 동시에 존재한다는 뜻이므로 하나를 함부로 고르면 안 된다.

```ts
const distinctSido = new Set(addrDocs.map(d => (d.address ?? d.road_address)?.region_1depth_name));
if (distinctSido.size > 1) {
  return { ambiguous: addrDocs.map(d => ({ label, lat, lng })) };
}
```

이 코드는 검색 결과들의 시/도 이름만 뽑아 중복 없는 집합(Set)으로 만든 뒤, 그 집합의
크기가 2개 이상이면(즉 서로 다른 시/도가 섞여 있으면) 자동으로 하나를 고르지 않고
`{ ambiguous: [...] }`(후보 목록) 응답을 돌려준다.

- 모호하다고 판단되면 도로 판정(`analyzeRoad()`)은 아예 호출하지 않고 곧바로 후보 목록을
  응답한다 — 어차피 위치가 확정 안 됐으니 판정 연산을 할 필요가 없어서 생략하는 것이다
- 클라이언트(`app/page.tsx`)는 이 응답을 받으면 결과 카드 자리에 후보 선택 목록을
  렌더링한다. 사용자가 후보 하나를 고르면, 이미 있던 `?lat=&lng=` 좌표 검색 경로
  (`search({ lat, lng })`)를 그대로 재호출해서 결과를 보여준다 — 이걸 위해 별도 API를
  새로 만들 필요 없이 기존 좌표 검색 경로를 재사용한 것이다
- 절대다수의 검색은 시/도가 하나로만 잡히므로, 이 로직을 거쳐도 기존과 똑같이 추가 왕복
  없이 바로 결과가 나온다

---

## 16. 데이터 갱신 가이드

이 프로젝트를 유지보수할 때 반복적으로 하게 될 작업들을, 상황별로 정리해뒀다.

### 직제 개편 시 (연 1회)
회사가 매년 새 직제세부운영계획을 배포하면, 그 내용을 반영해야 한다.
1. 새 hwpx 파일을 zip으로 압축 해제 → `Contents/section*.xml`에서 `<hp:t>` 텍스트를
   추출해 원문 표를 복원한다 (방법은 §4-3과 동일)
2. `lib/highway-jurisdiction.ts`의 `JURISDICTION_RULES`를 복원한 표와 전수 대조해서 수정한다
3. 검색창에 `노선명 km` 형태로(§2-0-1의 내부 검증 경로) 관할이 바뀌는 경계 지점들을 찍어보고,
   직제 원문 표와 눈으로 대조해 확인한다
4. `git push`로 배포한다

### OSM 도로 데이터 갱신 시
1. Geofabrik에서 최신 shp.zip을 다운로드한다
2. 변환 스크립트를 실행해 `public/data/osm-road-grid.json`을 다시 만든다
3. 자동 테스트(§13)를 돌려보고 이상 없으면 `git push`로 배포한다

### 도시고속화도로·민자 오류 신고 시
사용자가 특정 도로의 관할이 잘못됐다고 신고하면, `lib/road-rules.ts`의 해당 테이블
(`URBAN_EXPRESSWAYS` 또는 `PRIVATE_LOCAL_ROADS`)에 한 줄만 추가하면 된다 → `git push`

### ETC 도로중심선 갱신 시
1. 공공데이터포털에서 최신 CSV를 다운로드해 변환한다
2. **민자 보간 포인트(§4-1에서 우리가 직접 만든 좌표들)와 톨게이트·나들목 앵커 포인트(아래
   항목)는 이 변환 과정에서 사라지므로 별도로 백업해뒀다가 재생성 후 다시 병합해야 한다**
3. 자동 테스트(§13)를 돌려보고 이상 없으면 `git push`로 배포한다

### 톨게이트·나들목 오탐 신고가 계속될 때 (전국 일괄 재실행)
개별 신고 대응이 아니라 위 §4-1의 두 전국 일괄 보완 중 하나를 다시 돌리고 싶을 때.

**① 시설 앵커 재실행** (새 IC/JC/TG/SA가 `highway-nodes.json`에 추가됐을 때):
1. `highway-nodes.json`에서 고유 이름을 뽑아 카카오 키워드검색으로 전부 지오코딩 (908개
   기준 약 5분, 캐시에 저장해두고 재실행 시 이어서 진행)
2. 노드의 `routeName`과 문자열이 일치하는 기존 `highway-grid.json` 포인트를 찾아, 그
   포인트에서 3km 이내인 지오코딩 후보만 채택 → 그 포인트의 코드를 그대로 물려받아 새 앵커로 생성
3. 매칭 실패 항목(같은 노선명 grid 포인트 없음 / 3km 이내 후보 없음)은 목록화해서 §6 코드
   세분화 문제인지 개별 확인

**② 램프 형상 재실행** (OSM 램프 데이터가 갱신됐거나 처음 세팅할 때):
1. Geofabrik south-korea-latest-free.shp.zip 다운로드(577MB) → 압축 해제
2. `gis_osm_roads_free_1`에서 `fclass == 'motorway_link'`인 세그먼트만 추출
3. 각 세그먼트 중간 지점 기준으로 기존 `highway-grid.json` 포인트 중 1.5km 이내 최근접
   포인트를 찾아 그 코드를 물려받음 → 150m 간격 리샘플
4. 매칭 안 된 세그먼트(1.5km 이내에 아무 데이터도 없는 곳)는 애초에 우리가 안 다루는
   해외/무관 도로일 가능성이 높아 그대로 버림

①·② 모두 마지막은 동일: `highway-grid.json`에 병합 → 로컬 서버로 회귀 테스트(§13, 순천TG·
화도TG·마석IC·기흥동탄IC 등 과거 수정 사례 포함) → `git push`
